// Keys definition
keyUp = keyboard_check(vk_up)
keyDown = keyboard_check(vk_down)
keyRight = keyboard_check(vk_right)
keyLeft = keyboard_check(vk_left)

// Player states
switch(state) {
	case PLAYERSTATE.FREE: free() break
}